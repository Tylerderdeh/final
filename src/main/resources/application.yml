server:
  port: 8070

spring:
  application:
    name: lab2
  datasource:
    url: jdbc:postgresql://localhost:5432/test_test
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    database-platform: org.hibernate.dialect.PostgreSQLDialect
